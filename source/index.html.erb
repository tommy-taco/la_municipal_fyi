---
title: LA Municipal 2017 FYI
---

  <div class="jumbotron">
  <div class="container">
  <h2>Municipal.FYI</h2>
  <p>LA Municipal Elections 2017</p>
  </div>
  </div>
  
  <div class="container">
  	<h1>Ballot</h1>
  </div>
  <% data.election.races.each do |r| %>
    <div class="container">
        <h2><%= r.title %></h2>
        <div class="row">
	        <div class="description">
	        <p>This is a sample description of the position. <br> it will likely be very long and need to include other information.<br><br>AJ, make sure this is added to the data structure for each "r"</p><strong>Candidates</strong>
	        </div>
            <div class="col-md-12">
              <% r.candidates.each do |c| %>
                <%= partial("candidate", :locals => { :name => c.name, :title => c.title, :website => c.website, :image => c.image, :incumbent => c.incumbent} ) %>
              <% end %>
          </div>
        </div>
    </div>
    <% end %>

 
</div>
